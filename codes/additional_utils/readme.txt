# explanation

--> some utils

# create chromsome embeddings

per_chromosome_embeddings.py --> launch the per chromsome embedder, calling instructions in file
Species_lm_embeddings.py --> code from johann just the model class of the embedder wrapped
# make tobias bigwigs --> corrected now
# other utils
